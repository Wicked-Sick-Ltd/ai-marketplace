# Plugin vendor-support audit — 2026-09-17

> Scope: the owner-only `wizzo-twin` plugin is out of scope here; it is audited in the private `claude-repo`.

Audit of every shareable Wicked Sick agent plugin against the six vendors this marketplace serves (Claude Code, Codex, Cursor, Copilot, Gemini, Grok). Read-only snapshot taken 2026-09-17; file:line citations refer to the repos at that date. Produced for the expansion plan workstream A ("Codex, Cursor and Claude all working in Jarvis").

## A. Inventory (6 plugins)

| Plugin | Source repo | Version | Components | Claude-specific parts | Portable parts |
|---|---|---|---|---|---|
| **token-usage** | `Wicked-Sick-Ltd/token-usage` (own repo; pinned in `ai-marketplace/.claude-plugin/marketplace.json:26` at sha `67164452fb51eff2e76df65b937dd3e725b75103`, and in `claude-repo/.claude-plugin/marketplace.json:30-50` as a `url` source) | 0.6.1 (`.claude-plugin/plugin.json:3`) | 1 skill (`skills/report/SKILL.md`), 1 bundled stdio **MCP server** (`scripts/mcp_server.py`: `session_cost`, `history`, `insights`, `diff`, `top_consumers`), **hooks** (`hooks/hooks.json`: `Stop` + `SubagentStop` → `scripts/token_usage.py hook`), CLI (`scripts/token_usage.py`), pricing data (`data/pricing.json`). No slash commands, no agents. | `hooks/hooks.json` (Claude-only lifecycle events `Stop`/`SubagentStop`); `plugin.json` `mcpServers.token-usage.env` uses `${CLAUDE_PLUGIN_ROOT}` / `${CLAUDE_PROJECT_DIR}`; `skills/report/SKILL.md:5` `allowed-tools` hard-lists `mcp__plugin_token-usage_token-usage__*` / `mcp__token-usage__*` tool names; discovery logic reads `~/.claude/projects/<cwd-slug>/*.jsonl` and the Cowork sandbox mount `/sessions/*/mnt/.claude/projects/…` (`SKILL.md:58,104`). | The MCP server itself is a plain stdio JSON-RPC process (host-agnostic protocol); `scripts/token_usage.py` / `data/pricing.json` are pure Python, no Claude imports — reusable if repointed at a different host's transcript format. |
| **session-lifecycle** | `claude-repo`, `plugins/session-lifecycle/` | 0.1.0 (`plugin.json:3`) | 2 skills (`safe-shutdown`, `session-startup`), 2 slash commands (`commands/startup.md`, `commands/shutdown.md`), 1 shell script + prompt (`shutdown-session.sh`, `shutdown-prompt.md`). No hooks, no MCP, no agents. | `shutdown-session.sh:40,87,113,115` shells out to the **`claude` CLI binary** (`claude -p …` / `claude -p --continue …`) for a headless backoff-retry wrapper — Claude Code CLI-specific, but optional convenience tooling, not required for the skill/command bodies to make sense. | Skill bodies (git/gh/bash logic, reading `PORTFOLIO-STATE.md` + `.remember/`) are host-neutral; `commands/*.md` frontmatter is only `description` + `argument-hint` (no Claude-only keys) — Cursor discovers `commands/` too (`ai-marketplace/docs/cursor-integration.md:120-122`). |
| **pr-flow** | `claude-repo`, `plugins/pr-flow/` | 0.1.1 (`plugin.json:3`) | 1 skill (`landing-prs`), 1 slash command (`commands/pr.md`), 1 shell script (`skills/landing-prs/pr-wait.sh`). No hooks, no MCP, no agents. | `commands/pr.md:14-17` resolves the wait script via `${CLAUDE_PLUGIN_ROOT}/skills/landing-prs/pr-wait.sh` — the one Claude-only path expansion in this plugin. | `pr-wait.sh` is plain `bash` + `gh` + `jq` (`skills/landing-prs/pr-wait.sh:1-27`), no Claude dependency; the skill body is host-neutral prose. **Not mentioned in either existing cross-AI planning doc** (`docs/cursor-integration.md`, `traefik-laravel-forge/docs/cross-ai-marketplace-plan.md`) — those were written before this plugin existed or was catalogued; this row is my own direct-code assessment. |
| **estate-maintenance** | `claude-repo`, `plugins/estate-maintenance/` | 0.1.0 (`plugin.json:3`) | 3 skills (`deepsec-rollout`, `findings-sync`, `autonomous-maintenance`), 1 slash command (`commands/maintain.md`). No bundled hooks/MCP; heavy lifting is delegated to Python scripts in the separate `wicked-repo-inventory` repo (`skills/deepsec-rollout/SKILL.md:11,16`, `skills/findings-sync/SKILL.md:11,15`). | None hard-coded (no `CLAUDE_PLUGIN_ROOT`, no `allowed-tools`, no `mcp__` tool names anywhere in this plugin's files). Practically depends on the org's mesh coordination MCP server (Project Jarvis hub) and `gh`, described only in prose. | Skills/commands are plain Markdown; the actual scripts (`wicked-repo-inventory/deepsec-rollout.py`, `findings-sync.py`) are host-neutral Python + `gh`. |
| **onboarding** | `claude-repo`, `plugins/onboarding/` | 0.1.0 (`plugin.json:3`) | 1 skill (`verify-setup`), 1 slash command (`commands/onboard.md`), 1 shell script at repo root (`scripts/bootstrap.sh`, shared install entry point). | `scripts/bootstrap.sh:112-118` (`ensure_marketplace`, `install_plugin session-lifecycle/pr-flow/estate-maintenance/token-usage/wizzo-twin`) wraps the **Claude Code CLI's** `claude plugin marketplace add` / `claude plugin install` — this is the plugin's actual reason for existing, and it is entirely Claude-CLI-shaped. `scripts/bootstrap.sh:60` also checks `have claude`. | The `verify-setup` skill's checks (repo clone present, `.remember/` exists, 1Password access) are host-neutral prose and would still make sense elsewhere, just without the marketplace-install half. |
| **product-lifecycle** | `claude-repo`, `plugins/product-lifecycle/` | 0.1.0 (`plugin.json:3`) | 3 skills (`lifecycle-navigator`, `opportunity-scoring`, `register-hygiene`), **15 slash commands** (`commands/product-*.md`), 1 reference doc (`reference/research-rules.md`). No hooks, no bundled MCP, no agents. | None hard-coded — no `mcp__` tool names or `CLAUDE_PLUGIN_ROOT` found anywhere in this plugin (`grep -rl 'mcp__' plugins/product-lifecycle/` → no hits). Its usefulness on any host still depends on that host having Notion/HubSpot/Asana/Apollo MCP connectors wired in (10 of 16 command files reference Notion by name), but the plugin itself doesn't name a vendor-specific tool. | The prompt bodies are plain Markdown/English describing the Wicked Sick Product Lifecycle Framework — genuinely portable at the manifest level. |

Inventory count: **6 plugins** (plus the owner-only `wizzo-twin`, out of scope).

---

## B. Support matrix (6 plugins × 6 vendors = 36 cells)

Legend: **LISTED** = on a real, working marketplace index today · **CLAUDE-ONLY** = fundamentally depends on Claude/Cowork-only mechanics · **PORTABLE-WITH-WORK** = skill/MCP body could carry over, manifest work required.

| Plugin | Claude Code | Codex | Cursor | Copilot | Gemini | Grok |
|---|---|---|---|---|---|---|
| token-usage | **LISTED** — `ai-marketplace/.claude-plugin/marketplace.json:11-27` (sha-pinned) and `claude-repo/.claude-plugin/marketplace.json:30-50` | **CLAUDE-ONLY** — `ai-marketplace/gemini/README.md`-style caveat applies equally; `.agents/plugins/marketplace.json:1-7` lists nothing, and validate.py's `CLAUDE_ONLY = {"token-usage"}` (`scripts/validate.py:21`) explicitly forbids it appearing on Codex/Copilot | **CLAUDE-ONLY** — explicit in `docs/cursor-integration.md:151`: "Listing it on Cursor without a Cursor log parser is a lie" | **CLAUDE-ONLY** — same `CLAUDE_ONLY` guard (`scripts/validate.py:134-142`) | **CLAUDE-ONLY** — `gemini/README.md:9`: "not a Gemini extension until that repo ships `gemini-extension.json` and a Gemini-relevant runtime" | **LISTED** — reads the Claude catalog directly (`ai-marketplace/README.md:20`, `AGENTS.md` line "Grok already reads Claude Code marketplaces… with no extra packaging" per `cross-ai-marketplace-plan.md:27`) |
| session-lifecycle | **LISTED** — `marketplace.json:51-70` | PORTABLE-WITH-WORK | PORTABLE-WITH-WORK — docs call it the model first port (`docs/cursor-integration.md:146`) | PORTABLE-WITH-WORK | PORTABLE-WITH-WORK | **LISTED** |
| pr-flow | **LISTED** — `marketplace.json:71-92` | PORTABLE-WITH-WORK | PORTABLE-WITH-WORK | PORTABLE-WITH-WORK | PORTABLE-WITH-WORK | **LISTED** |
| estate-maintenance | **LISTED** — `marketplace.json:93-114` | PORTABLE-WITH-WORK | PORTABLE-WITH-WORK — `docs/cursor-integration.md:147` | PORTABLE-WITH-WORK | PORTABLE-WITH-WORK | **LISTED** |
| onboarding | **LISTED** — `marketplace.json:115-133` | PORTABLE-WITH-WORK (partial — see notes) | PORTABLE-WITH-WORK — docs label it "Partial" (`docs/cursor-integration.md:149`) | PORTABLE-WITH-WORK (partial) | PORTABLE-WITH-WORK (partial) | **LISTED** |
| product-lifecycle | **LISTED** — `marketplace.json:134-155` | PORTABLE-WITH-WORK | PORTABLE-WITH-WORK — `docs/cursor-integration.md:148` | PORTABLE-WITH-WORK | PORTABLE-WITH-WORK | **LISTED** |

**Cell counts:**
- LISTED: **12** (6 × Claude, 6 × Grok)
- CLAUDE-ONLY: **4** (token-usage × Codex/Cursor/Copilot/Gemini)
- PORTABLE-WITH-WORK: **20** (5 plugins × Codex/Cursor/Copilot/Gemini)
- INSTALLABLE-BUT-UNLISTED: **0**
- N/A: **0**
- Total: 42 ✓

Important qualifier on the Claude column: `ai-marketplace`'s own `.claude-plugin/marketplace.json` today lists **only `token-usage`** (`ai-marketplace/.claude-plugin/marketplace.json:9-28`); the other 6 plugins are "LISTED" via `claude-repo`'s own working marketplace (`claude-repo/.claude-plugin/marketplace.json`), not via the org catalog repo. This is called out as deliberate/current-state in `ai-marketplace/README.md:40`: "Workflow skills … live in claude-repo today with Claude manifests only." It is a real, cited fact, not an inconsistency I'm introducing.

Important qualifier on the Grok column: there is no `.grok-plugin/marketplace.json` anywhere (omitted "on purpose", `ai-marketplace/README.md:20`); Grok's "LISTED" status is Grok reading the Claude catalog natively, not a separate index.

---

## C. Per-vendor gaps — what a plugin needs to appear on that index

**Claude Code** — satisfied for all 7 today (each has `.claude-plugin/plugin.json` under `claude-repo/plugins/<name>/`, and a `claude-repo/.claude-plugin/marketplace.json` entry). Gap: cross-listing the 6 workflow plugins into `ai-marketplace`'s own Claude index hasn't happened yet — not required for them to work, but it is the stated end state (`ai-marketplace/AGENTS.md:2-3`: "This repository is an index… it does not contain plugin code").

**Codex** (`.agents/plugins/marketplace.json`) — currently `{"name":"wickedsick","interface":{"displayName":"Wicked Sick"},"plugins":[]}` (`ai-marketplace/.agents/plugins/marketplace.json:1-7`). `scripts/validate.py:138-142` treats Codex identically to Claude: `plugin_names(..., require_sha=True)` — a full 40-char lowercase-hex `sha` plus a GitHub `repo`, same shape as Claude (`scripts/validate.py:41-62`). **Critically, that schema has no subdirectory/path field** — it assumes the pinned repo *is* the plugin root (exactly how `token-usage` works). None of `session-lifecycle`, `pr-flow`, `estate-maintenance`, `onboarding`, `product-lifecycle`, or `wizzo-twin` live in their own repo — they're all `claude-repo/plugins/<name>/`. So listing any of them on Codex today would require either (a) extracting each into its own repo, or (b) extending the catalog schema with a subdirectory field (not present, and `validate.py` would need a matching change). Each plugin also needs a root Agent Plugins `plugin.json` (`$schema` from agent-plugins.org) — none exist anywhere in `claude-repo` (`find . -name plugin.json` only finds the nested `.claude-plugin/plugin.json` files). `codex-repo` itself carries **no plugin/skill content at all** — it is Codex CLI approval/permission configuration only (`config/permissions.toml`, `config/permissions.md`, `rules/portable.rules`) and never mentions `claude-repo`, `ai-marketplace`, `SKILL.md`, or Agent Skills.

**Cursor** (`.cursor-plugin/marketplace.json`) — currently `{"name":"wickedsick", "plugins": []}` with a `metadata.description` explaining it's a stub (`ai-marketplace/.cursor-plugin/marketplace.json:1-11`). Unlike Codex/Copilot, Cursor's own schema is **path-based within an imported git repo** (`docs/cursor-integration.md:53-57`); `scripts/validate.py:65-114` (`_cursor_source_path`, `cursor_plugin_names`) explicitly **rejects** a GitHub `sha`/`repo` source for a Cursor entry (`scripts/validate.py:70-74`) and requires each resolved `plugin_dir` to contain `.cursor-plugin/plugin.json` **or** `plugin.json` (`scripts/validate.py:107-113`) — neither exists for any of the 5 `claude-repo` plugins today. So Cursor does **not** have the "no subdirectory" problem Codex/Copilot have — the fix is (1) add `.cursor-plugin/plugin.json` per plugin dir, (2) add a Cursor catalog **inside `claude-repo`** with `metadata.pluginRoot: "plugins"` (worked example at `docs/cursor-integration.md:79-91`), (3) import `claude-repo` itself as the Team Marketplace (`docs/cursor-integration.md:72-77`). Any future stdio MCP entries need `${CURSOR_PLUGIN_ROOT}` in place of `${CLAUDE_PLUGIN_ROOT}` (`docs/cursor-integration.md:44-46`) — not currently an issue since `wizzo-twin`'s only MCP servers are remote HTTP.

**Copilot** (`.github/plugin/marketplace.json`) — currently `{"name":"wickedsick","metadata":{...},"plugins": []}` (`ai-marketplace/.github/plugin/marketplace.json:1-12`). Same `require_sha=True` treatment and same repo-root-only limitation as Codex (`scripts/validate.py:138-142`). Also needs the user/org-level VS Code setting `"chat.plugins.marketplaces": ["Wicked-Sick-Ltd/ai-marketplace"]` (`ai-marketplace/README.md:18`) — a client config change, separate from the catalog file. Copilot-only extras (agents/hooks) would go under a `com.github.copilot/` namespace per the plan (`cross-ai-marketplace-plan.md:100`), unused today.

**Gemini CLI** — no catalog file at all; a plugin needs `gemini-extension.json` at **the plugin repository root** (`ai-marketplace/gemini/README.md:3-6`; `cross-ai-marketplace-plan.md:99,117`), and install is `gemini extensions install <github-url>`. Same repo-root assumption as Codex/Copilot — no documented subdirectory install path, so the 6 `claude-repo`-hosted plugins face the same "needs its own repo" question (unverified either way in the sourced docs; flagged as an open question in §E). Progress is tracked as a manual row in `gemini/README.md`, not JSON.

**Grok** — no gap for any already-Claude-listed plugin; it reads `.claude-plugin/` natively (`ai-marketplace/README.md:20`, `AGENTS.md`). A dedicated `.grok-plugin/marketplace.json` is explicitly out of scope unless a Grok-only plugin appears (`cross-ai-marketplace-plan.md:62`, "optional; only if we want Grok-native listing").

---

## D. Recommended porting order (cheapest / highest-value first)

| # | Plugin | Effort | Why |
|---|---|---|---|
| 1 | **session-lifecycle** | **S** | Zero Claude-only bindings in the skill/command bodies; only friction is the optional `claude` CLI shell-out inside `shutdown-session.sh` (a convenience wrapper, not required for the skill itself). Already the docs' own pick for "first portable skill" / "Best first Team Marketplace listing" (`docs/cursor-integration.md:146`). |
| 2 | **pr-flow** | **S/M** | One `${CLAUDE_PLUGIN_ROOT}` reference to generalise (`commands/pr.md:16`); everything else is plain `bash`+`gh`+`jq`. Highest daily-use value in the whole set — `claude-repo/README.md:31` calls it "Required on every fleet box." Not covered by either existing planning doc, so this row is my own direct-code assessment, not a repeat of prior guidance. |
| 3 | **estate-maintenance** | **M** | No hard-coded Claude bindings, but real usefulness depends on the mesh coordination MCP server and `wicked-repo-inventory` scripts being reachable — an environment/integration cost on top of the manifest work. Second in the docs' own checklist (`docs/cursor-integration.md:147,188`). |
| 4 | **product-lifecycle** | **M/L** | Fully portable prose at the manifest level, but its actual usefulness on any other host requires that host to already have Notion/HubSpot/Asana/Apollo MCP connectors configured — an org/connector dependency, not a code change. 15 commands also means more manifest and test surface than the others. |
| 5 | **onboarding** | **M** | Split personality: `verify-setup` skill ports cheaply; the plugin's actual purpose (`scripts/bootstrap.sh:112-118` wrapping `claude plugin marketplace add`/`install`) is Claude-CLI-only. Porting means either shipping a diminished skill-only listing on other hosts, or writing a second bootstrap script per host — real duplicate engineering, which is why the docs call it "Partial" (`docs/cursor-integration.md:149`). |
| 6 | **token-usage** | **L — and arguably shouldn't be "ported" at all** | Hooks (`Stop`/`SubagentStop`) and the whole ledger model are Claude Code/Cowork session-shaped (`~/.claude/projects/*.jsonl`, Cowork sandbox mounts). This isn't a manifest exercise — it needs a from-scratch transcript parser per host, and if a host has no Stop-equivalent hook, the "always up to date" behaviour can't be reproduced at all. **Recommended honest listing text for every non-Claude/Grok vendor:** *"Claude Code & Cowork transcripts only — not available on this platform until a \[Cursor/Codex/Copilot/Gemini\] session-log parser exists."* This matches the plan's own stance (`ai-marketplace/README.md:38`, `cross-ai-marketplace-plan.md:104,163`) and `scripts/validate.py`'s `CLAUDE_ONLY` guard (`scripts/validate.py:21,134-142`), which already hard-blocks it from appearing on Codex/Copilot and would need the same fix applied to Cursor/Gemini indexes if they ever grow beyond stubs.

**Inherently Claude-only:** `token-usage` (hooks + transcript format). Everything else in the inventory is portable in principle; the blockers are missing manifests (all 6) and, for Codex/Copilot/possibly Gemini, the monorepo-subdirectory limitation in §C.

---

## E. Open questions for Craig

1. **Should `ai-marketplace` or `codex-repo` host the Codex plugin packs?** `codex-repo` today contains zero plugin/skill content — it's pure Codex CLI approval/permission config (`approval_policy`, `sandbox_mode`, `rules/portable.rules`) and never references `claude-repo`, Agent Skills, or `SKILL.md`. The `.agents/plugins/marketplace.json` catalog file already lives in `ai-marketplace` per the existing plan (`cross-ai-marketplace-plan.md:60-64`), which argues for keeping Codex *packs* there too and leaving `codex-repo` as pure machine/approval config — but that's a call worth confirming explicitly before building it out, since `codex-repo`'s `README.md`/`AGENTS.md` give no signal either way.
2. **Extraction vs. schema change for Codex/Copilot.** Given the repo-root-only limitation in §C, do you want to (a) split some/all of the 5 `claude-repo` plugins into their own repos (mirrors `token-usage`'s shape, costs repo sprawl), or (b) extend `ai-marketplace/scripts/validate.py` + the catalog schema with a subdirectory field for Codex/Copilot entries (keeps them in `claude-repo`, but diverges from Claude's own marketplace schema, which also doesn't support it)?
3. **Cursor Team Marketplace import target.** `docs/cursor-integration.md` recommends importing `claude-repo` directly as the Team Marketplace (`docs/cursor-integration.md:70-77`) rather than vendoring anything into `ai-marketplace`. Confirm that's still the intended target before anyone connects it in the Cursor dashboard, and confirm who gets **Default On** vs **Default Off/Required** per plugin (`docs/cursor-integration.md:172-175` proposes Default On for `session-lifecycle`, owner-only for `wizzo-twin`).
4. **Gemini subdirectory support is unverified.** Neither sourced doc says whether `gemini extensions install <url>` accepts a subpath into a monorepo, or requires the repo root to *be* the plugin. Worth a 10-minute manual test against `claude-repo` before assuming extraction is required.
5. **`pr-flow` was never assessed in either planning doc.** It postdates (or was simply omitted from) `docs/cursor-integration.md` and `cross-ai-marketplace-plan.md`. Given it's flagged "Required on every fleet box" (`claude-repo/README.md:31`) and is one of the cheapest ports (§D #2), should the Cursor-integration doc be updated to include it explicitly, so it doesn't fall through the cracks of the existing checklist (`docs/cursor-integration.md:182-192`)?
6. **`onboarding`'s non-Claude story.** Do you want a second, host-native bootstrap script per vendor (real engineering cost), or is a skill-only "verify, don't install" listing (as `docs/cursor-integration.md:149` already suggests) good enough outside Claude?
