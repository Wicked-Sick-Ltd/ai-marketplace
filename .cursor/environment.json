{
  "name": "ai-marketplace",
  "install": "cp -n .env.example .env; npm install && npx prisma generate && npx prisma migrate deploy && npm run db:seed",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev"
    }
  ],
  "ports": [
    {
      "name": "web server",
      "port": 3000
    }
  ]
}
